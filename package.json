{
  "name": "carapace-js",
  "private": true,
  "version": "0.1.0",
  "description": "JavaScript SDKs and middleware for Carapace.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "node -e \"console.log('lint not configured yet')\"",
    "test": "node -e \"console.log('tests not configured yet')\""
  }
}

